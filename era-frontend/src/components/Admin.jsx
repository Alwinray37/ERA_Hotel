// here, admin can see all rooms, add new rooms, and delete existing rooms
// can see guests and their reservations
// need to create login form

export default function Admin() {
    return (
        <div className="admin-container main-content">
            <h2>Admin Dashboard</h2>
            <p>Manage rooms, reservations, and guests here.</p>
            {/* Add components for managing rooms and reservations
                - add a form for log login
                - add two inputs
                - add button
                - function to handle submit, save admin obj to a variable

                */}
        </div>
    );
}